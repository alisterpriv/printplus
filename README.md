
  # Windows app

  This is a code bundle for Windows app. The original project is available at https://www.figma.com/design/fceCJX8BI1rCdSGuDsPlbR/Windows-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  